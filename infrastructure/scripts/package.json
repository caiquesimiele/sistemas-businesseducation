{
  "name": "infrastructure-scripts",
  "version": "1.0.0",
  "description": "Scripts de infraestrutura (migrations, seeds)",
  "main": "seed-database.js",
  "scripts": {
    "seed:staging": "node seed-database.js --env staging",
    "seed:production": "node seed-database.js --env production"
  },
  "dependencies": {
    "pg": "^8.11.3"
  }
}

